import Foundation

// This file extends AppDashboardViewModel. The base type is defined in a separate file.
// NOTE: This file name intentionally differs from the base type's file to avoid build-time
// collisions like "Multiple commands produce ... DashboardViewModel.stringsdata" which can
// occur when multiple files share the same basename.
extension AppDashboardViewModel {
    // Additional helpers or computed properties can live here to avoid duplicate type declarations.
}
